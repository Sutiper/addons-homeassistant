name: "SSH Backup"
description: "Serveur SSH de secours avec support SFTP, clé publique et mot de passe"
version: "1.0.0"
slug: "ssh_backup"
url: "https://github.com/tgillier/addons-homeassistant"
arch:
  - amd64
  - aarch64
  - armv7
startup: services
boot: auto
init: false

ports:
  22/tcp: 22223
ports_description:
  22/tcp: "Port SSH de secours"

map:
  - type: data
    read_only: false
  - type: ssl
    read_only: true

options:
  authorized_keys: []
  password: ""
  auth_mode: "key_or_password"
  allow_root_login: false
  allow_tcp_forwarding: false
  allow_agent_forwarding: false
  sftp_enabled: true
  sftp_only: false
  log_level: "INFO"
  banner: ""

schema:
  authorized_keys:
    - str
  password: password
  auth_mode: "list(key_only|password_only|key_or_password)"
  allow_root_login: bool
  allow_tcp_forwarding: bool
  allow_agent_forwarding: bool
  sftp_enabled: bool
  sftp_only: bool
  log_level: "list(DEBUG|INFO|WARNING|ERROR)"
  banner: str

stage: stable
